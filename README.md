# aravindvrm.github.io

Personal site. Static HTML, no framework, no build step.

- `index.html` — the whole site
- Deployed from `main` at the repository root via GitHub Pages

Project writeups live in their own repositories and are served from
`aravindvrm.github.io/<project>/`.
